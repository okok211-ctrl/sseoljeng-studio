# 썰쟁 Studio v17.6 Auto Generate + Storage Quota Fix

- 60문단 검사 통과 후 제작 자료 전체 생성 자동 시작
- 자동 시작 시 재검사 재귀 방지
- 캐릭터 얼굴 Base64를 프로젝트 sseoljeng-v8 저장본에서 제외
- 얼굴은 별도 저장소 sseoljeng-character-photos-v1에 보존
- 프로젝트 불러오기 시 얼굴 자동 재결합
- localStorage 용량 초과가 나도 생성 자체는 실패 처리하지 않음
- v17.5의 캐릭터 5·6 얼굴 유지, CTA, CTR, 쇼츠/썸네일/이미지 기능 유지

# 썰쟁 Studio v17.5 Character Photo Persistence Fix

- v17.4 기준 수정
- 캐릭터 슬롯 1~6을 저장/복원 대상으로 고정
- 5번·6번 캐릭터 얼굴 이미지가 로컬 저장/불러오기 후 사라지는 문제 방지
- 클라우드 프로젝트 저장/복원 시 이미지 필드(photo/image/avatar) 보존
- 부분 저장 데이터가 들어와도 기존 얼굴 사진을 덮어쓰지 않도록 보호
- 향후 6개 이상 캐릭터 데이터가 존재하면 강제로 잘라내지 않음
- 기존 CTA, 유튜브 CTR, 쇼츠/썸네일/이미지 엔진 유지

# 썰쟁 Studio v17.4 CTA Stack Fix

- v17.3 기준 수정
- Maximum call stack size exceeded 원인이 된 CTA 재귀/공통 주입 제거
- 시작 CTA는 openingPrompt에만 적용
- 엔딩 CTA는 endingPrompt에만 적용
- 설계도/이미지/쇼츠/썸네일/유튜브 정보 생성과 완전 분리
- 기존 v17.3 기능 유지

# 썰쟁 Studio v17.2 YouTube CTR Info Engine

- 감정 → 상황 → 궁금증 제목 규칙
- 반전 소재 선공개 금지
- 제목 후보 5개 + BEST 1개
- 썸네일 문구 3개
- 설명 첫 2줄 감정 우선
- 해시태그 5~6개
- 조회수 최적화 휴리스틱 점수 100점
- 85점 이상 추천
- 스포일러·소재 선공개·긴 제목·약한 감정 위험 요소 검사
- 실제 CTR 퍼센트 예측은 하지 않음

# 썰쟁 Studio v17.1 Thumbnail 4 People + Shorts Applied

- 썸네일 인물 수 기본값: AI 자동 추천 1~4명
- 최대 1명, 2명, 3명, 4명 직접 선택 가능
- 선택값을 실제 썸네일 프롬프트에 직접 연결
- 조회수형 쇼츠 생성 프롬프트에 상황형 다인물 규칙 직접 연결
- 쇼츠 4컷 동일 여주 단독 반복 방지
- 롱폼 이미지 프롬프트에도 상황형 다인물 규칙 직접 연결
- 기존 로그인, 클라우드, Vrew, 음성용, SRT 유지

# 썰쟁 Studio v17.0 Stable Situation Image Engine

## 기준 원본
- v16.6.1 Consistency Fix를 기준으로 제작
- 로그인, Firebase, 프로젝트 저장/불러오기, Vrew, 음성용, SRT, 기존 UI 유지

## 이미지 엔진 업그레이드
- 썸네일: 상황형 갈등 장면, 핵심 인물 2~3명 우선, 영화 포스터형 구도
- 롱폼 이미지: 문단 상황에 따라 1~4명 자동 선택, 행동 중심 장면, 앵글 다양화
- 쇼츠 이미지: 컷별 사건 진행에 맞춰 등장인물 구성 변경, 말풍선 금지
- 공통: 인물별 표정·시선·손동작·거리·소품·조명 자동 설계
- 콜라주·분할 화면·단체사진 구도·같은 여주 혼자 반복 금지

# 썰쟁 Studio v16.5 Prompt Quality Edition

## 변경 핵심
- 품질 검사 탭, 자동 품질 검사, 선택 문단 재생성 기능을 제거했습니다.
- 별도 품질 검사 API 호출 없이 롱폼 생성 프롬프트 단계에서 반복 방지 규칙을 적용합니다.
- 같은 감정·설명·사건 반복 금지, 문단마다 새로운 정보 또는 행동 진행, 마지막 5문단 역할 분리 규칙을 유지합니다.
- 마지막 문단은 새 일상 장면 → “오늘 이야기는 어떠셨나요?” → 공감 질문 → 댓글 → 구독·좋아요 → 감사 인사 순서로 마무리합니다.
- 기존 캐릭터, 이미지, 쇼츠, 썸네일, SRT, 음성용, 프로젝트 저장 기능은 유지됩니다.

# 썰쟁 Studio v16.2 선택 문단 품질 수정

## 새 기능
- 왼쪽 `🧠 품질 검사` 버튼 또는 상단 `품질 리포트` 탭에서 현재 원고를 다시 검사합니다.
- 중복 감정, 사건 재요약, 정보 밀도 부족, 마지막 5문단 역할 중복, 엔딩 누락을 문단 번호와 함께 표시합니다.
- 수정 권장 문단은 체크박스로 자동 표시됩니다.
- `선택 문단만 재생성`을 누르면 선택한 문단만 AI가 다시 작성합니다.
- 선택하지 않은 문단은 원문 그대로 유지합니다.
- 재생성 시 앞뒤 문단과 설계도를 함께 참고해 연결이 끊기지 않도록 합니다.
- 수정 후 SRT와 음성용 원고도 자동 갱신합니다.

## 사용 순서
1. 롱폼 원고를 생성하거나 붙여넣습니다.
2. `🧠 품질 검사`를 누릅니다.
3. `품질 리포트` 탭에서 수정 권장 문단을 확인합니다.
4. 고칠 문단만 체크합니다.
5. `✨ 선택 문단만 재생성`을 누릅니다.
6. 수정 결과가 기존 롱폼 원고에 자동 반영됩니다.
